service:
  type: ClusterIP

persistence:
  enabled: true
  size: 20Gi
  storageClass: local-path

ingress:
  enabled: true
  className: traefik
  hosts:
    - host: forgejo.homenet.meow
      paths:
        - path: /
          pathType: Prefix

gitea:
  admin:
    username: meow-admin
    password: "super-secret-password"
    email: admin@example.com
